# 验证记录

日期：2026-09-12。环境：macOS，Node.js 20.20.2；运行时不需要 npm 第三方依赖。

## 已执行

| 检查 | 结果 | 能说明什么 |
| --- | --- | --- |
| `npm test` | 30 / 30 通过 | 22 个规则/数据场景、7 个费用提示/筹码统计/音效场景及 1 个 HTTP 集成场景；包含非法行动、暗牌、过期请求、来源限制与 4 人配置 |
| `npm run simulate -- --games 30` | 30 / 30 完成；2、3、4 人各 10 局 | 每步检查发展卡、贵族、筹码守恒；终局后完整回放一致 |
| `python3 scripts/audit-data.py` | 90 卡和 10 贵族均零差异 | 固定版本社区数据多表交叉核对；详情见 `data-audit.json` |
| `npm run assets` | 106 / 106 SHA-256 与长度匹配 | 本地原图未被改变；另对一项固定版本远端下载做了哈希验证 |
| JavaScript 语法检查 | 通过 | 引擎、服务与浏览器代码可以解析 |
| 本地 HTTP 首页 | 200，图片与脚本正常显示 | 本机可启动并访问 |

自动对局使用 `validation-0` 至 `validation-29` 种子，并轮换先手。均以实际分数结束，未用测试决策预算截断判胜。对手只是确定性验证策略，不代表训练或棋力评估。

## 验证边界

- 单元测试依据规则手工构造；完整复盘与守恒属于自洽验证，还没有独立官方裁判逐步对照。
- 数据交叉来源是社区记录，不是官方机器可读牌库。
- UI 原图可加载，不代表全部 100 个卡面均已与实体牌人工签核。
- 在线安全、多人联网、压力负载、训练算法和跨设备同步不在此次范围。
- 当前本机服务是单进程内存状态；停止服务不会保留未完成对局。

## 浏览器验证

使用独立的 `127.0.0.1:4174` 临时牌桌，保留用户的 `4173` 对局。

- 桌面版：经典卡图、贵族、宝石、三层市场与玩家面板正常呈现。
- 拿两枚同色筹码后正确交接给下一位玩家。
- 暗抽预留后对应牌堆减少一张、获得黄金，下一玩家看到的预留为暗牌。
- 拿取三色后通过购买弹窗支付白 2 蓝 1：牌进入已购区、绿色折扣增加、筹码回池、市场补牌。
- 新建 4 人对局并指定玩家 3 先手：5 位贵族、每色 7 枚、先手交接正确。
- WebMCP 读取与行动工具已注册；合法暗抽能完成同一状态更新；未打开交接幕帘时执行行动被拒绝。
- 原始桌面对局未重置；临时测试牌桌与服务在验证后关闭。

本次没有运行手机实机或专门窄屏截图验证；响应式样式已提供，但不把它记为已验证。

## 音效与辅助提示增强

- 30 项测试全部通过；再次完成 30 局 2–4 人模拟与复盘验证。
- 费用提示对全部 90 张卡、75 步实际对局状态与规则引擎支付判定逐项比对，包括共享黄金池、免费牌和折扣不能为负。
- 总数测试包含黄金、恰好 10 枚、超过上限；声音计划检查无黄金可拿、免费购买和贵族到访不重复播放。
- 14 个 WAV 文件哈希、格式和 HTTP 音频类型通过；文件均有有效非静音样本、无数字削波。浏览器已完成全部解码、试听触发和静音禁用检查，无控制台警告或错误。
- 在独立 4174 牌桌拿取、暗抽和购买：玩家总数分别正确显示 5 / 10、含两枚黄金的 2 / 10；购入红色发展卡后，同一张蓝色卡在玩家 1 的提示中扣除红色费用，玩家 2 仍需支付红色费用，并正确显示两枚黄金抵扣后的缺额。
- 购买弹窗的原价、折扣、应付、持有和还缺明细与牌面提示一致。测试保持 4173 的原对局不变。

## 桌面横向布局

- 宽度至少 1100px 时扩展牌桌可用宽度，市场卡牌与费用提示左右排列，压缩贵族区及玩家面板间距；较窄窗口保留原响应式布局。
- 浏览器实际窗口 1326 × 1104：12 张市场卡与费用提示、两位玩家信息同时可见；文档宽度等于窗口宽度，无横向溢出。保留原对局，未执行游戏行动。
- 30 项测试通过，30 局模拟完成。该次调整只修改布局样式。

## 超额拿取取消

- 超额拿取先打开归还预选弹窗，用户确认前不提交行动；取消、关闭或 Escape 返回选择阶段。
- 浏览器验证：持有 9 枚、选择拿 3 枚时出现预选弹窗；取消后服务端观察、修订号和合法行动与操作前完全一致。再次选择并预选归还 2 枚，确认后持有 10 枚，正确交接下一位玩家。
- 30 项测试和 30 局模拟通过。提交后的拿取与归还仍由原规则引擎分别验证；若请求中断，已有归还阶段仍可手动完成。
